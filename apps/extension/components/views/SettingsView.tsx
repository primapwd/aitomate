export default function SettingsView() {
  return (
    <section>
      <p style={{ fontSize: 13, color: '#555' }}>
        LLM provider config, DB connector profiles, bridge pairing, and
        encrypted config import/export land in Phase 3 / Milestone 2
        (T3.3, T5.2, T5.4). The encrypted vault backing this view is already
        in place (T1.3).
      </p>
    </section>
  );
}
